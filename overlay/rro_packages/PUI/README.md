# PUI system icons

These resources come from `PUI Theme For Stock Android` v17.0.218 by 天伞桜.
Only overlays that replace system icons are included. They are built as mutable RROs so the
Interface settings page can switch the complete set at runtime.
